# The ray-tracing environment.
#
# `docs/` proper is the Documenter build and stays on the released Makie stack;
# path tracing needs Makie's `sd/lava` branch and the Vulkan packages under it,
# which is a different resolution of the same graph.
#
# This is deliberately NOT a workspace member. A workspace shares one manifest
# across every member, so putting this in it resolved the whole repo — `docs`
# included — against branch Makie, and the released GLMakie and WGLMakie that
# the documentation builds with do not work against it: they call
# `add_computation!(graph, scene, Val(:...))` where the branch takes
# `Symbol...`, so `using GLMakie` fails to precompile. One shared manifest
# cannot hold both stacks, and the docs build is not worth moving onto an
# unreleased branch to gain one. So this project resolves its own.
#
# The cost of standing alone is that every non-registry package anywhere in the
# graph has to be named below, not just the direct dependencies: Julia honours
# `[sources]` from the top-level project only, so the ones `DiscreteGlobalGrids`
# and `GlobalRegridding` declare for themselves are not consulted from here.
# That is why `ConservativeRegridding` and `GlobalRegridding` appear as deps at
# all — nothing in this environment calls them; they are here so that the
# sources naming them are legal.
#
#     julia -t auto --project=docs/raytracing examples/river_flythrough_raytraced.jl
#
# The `[sources]` below are RayDemo's: the ray-tracing stack is a set of
# branches that only resolve together, so they are pinned as a set rather than
# one at a time.
#
# Three of them are this example's rather than RayDemo's, and are unmerged
# fixes it needs. Repoint each at whatever carries the fix once it lands:
#
#   RayMakie  fix/recipe-forwarded-material-color   MakieOrg/Makie.jl#5760
#             `extract_material` dropped the colour of a material that reaches
#             a plot as a recipe-forwarded graph edge rather than an input, so
#             the terrain -- `dggsurface!` is such a recipe -- rendered flat
#             grey instead of by its colormap.
#   Hikari    as/hw-aux-normals                     JuliaGraphics/Hikari.jl#40
#             Stacked on #40, so it carries both: #40 threads `sbt_offset`
#             through the N-instance `push!`, without which the beads lose
#             their materials under `hw_accel`; the branch's own commit gives
#             the denoiser real normals and unmirrors its aux buffers.
#   Lava      as/refit-preserve-custom-index        SimonDanisch/Lava.jl#9
#             A TLAS refit restamped every instance's `custom_index` from the
#             batch's, which is 0, and that index is how a shader finds the
#             instance's material -- so a video's materials went wrong from
#             the first refit on.
#
# `RayMakie` is a second rev of the repository `Makie` and `ComputePipeline`
# already come from. That is not a second Makie: the branch forks off `sd/lava`
# and touches only `RayMakie/`, so `Makie/` and `ComputePipeline/` are
# identical at the two revs.

[deps]
ArchGDAL = "c9ce4bd3-c3d5-55b8-8973-c0e20141b8c3"
Colors = "5ae59095-9a9b-59fe-a467-6f913c188581"
ComputePipeline = "95dc2771-c249-4cd0-9c9f-1f3b4330693c"
ConservativeRegridding = "8e50ac2c-eb48-49bc-a402-07c87b949343"
DimensionalData = "0703355e-b756-11e9-17c0-8b28908087d0"
DiscreteGlobalGrids = "443be784-8f3c-41ab-9ff0-108bddc7e690"
DiscreteGlobalGridsVisualization = "26394bad-8505-4b24-ad04-fcf267e3a913"
Extents = "411431e0-e8b7-467b-b5e0-f676ba4f2910"
FileIO = "5789e2e9-d7fb-5bc7-8068-2c6fae9b9549"
FlyThroughPaths = "c11bb9a7-2755-425a-88f3-ebe93bbdb91f"
GeoInterface = "cf35fbd7-0cd7-5166-be24-54bfbe79505f"
GeoMakie = "db073c08-6b98-4ee5-b6a4-5efafb3259c6"
GeometryBasics = "5c1252a2-5f33-56bf-86c9-59e7332b4326"
GeometryOps = "3251bfac-6a57-4b6d-aa61-ac1fef2975ab"
GeometryOpsCore = "05efe853-fabf-41c8-927e-7063c8b9f013"
GlobalRegridding = "76847d5f-3a3e-4aa5-90cc-2a3a47dd1c11"
Geomorphometry = "714e3e49-7933-471a-9334-4a6a65a92f36"
Hikari = "afc56b53-c9a9-482a-a956-d1d800e05558"
ImageShow = "4e3cecfd-b093-5904-9786-8bbb286a6a31"
KernelInterfaces = "ce25d451-9bb5-4870-a156-ce22182641de"
Lava = "3a680b1f-cb25-4bee-9cf7-bc880b76dc8c"
LinearAlgebra = "37e2e46d-f89d-539d-b4ee-838fcccc9c8e"
Makie = "ee78f7c6-11fb-53f2-987a-cfe4a2b5a57a"
Mantle = "8f4c1f2e-6a3d-4b71-9c05-2e7d8a1b6f34"
RasterDataSources = "3cb90ccd-e1b6-4867-9617-4276c8b2ca36"
Rasters = "a3a2b9e3-a471-40c9-b274-f788e487c689"
RayMakie = "70852e3a-d594-4041-8350-783a02753e02"
Raycore = "afc56b53-c9a9-482a-a956-d1d800e05559"
StaticArrays = "90137ffa-7385-5640-81b9-e52037218182"
Statistics = "10745b16-79ce-11e8-11f9-7d13ad32a3b2"
Vulkan = "9f14b124-c50e-4008-a7d4-969b3a6cd68a"
VulkanCore = "16167f82-ea26-5cba-b1de-ed6fd5e30a11"

[sources]
ComputePipeline = {rev = "sd/lava", subdir = "ComputePipeline", url = "https://github.com/MakieOrg/Makie.jl.git"}
ConservativeRegridding = {rev = "e8fc67f420da0362d59b354e2db6fcde8107fec0", url = "https://github.com/JuliaGeo/ConservativeRegridding.jl"}
DiscreteGlobalGrids = {path = "../.."}
DiscreteGlobalGridsVisualization = {path = "../../lib/DiscreteGlobalGridsVisualization"}
GeometryOps = {rev = "697c7cc81b3d61666cf42bf697bf186cd7f6b2e2", url = "https://github.com/JuliaGeo/GeometryOps.jl.git"}
GeometryOpsCore = {rev = "main", subdir = "GeometryOpsCore", url = "https://github.com/JuliaGeo/GeometryOps.jl.git"}
GlobalRegridding = {path = "../../lib/GlobalRegridding"}
Geomorphometry = {rev = "clipped-neighbors", url = "https://github.com/asinghvi17/GeoArrayOps.jl.git"}
Hikari = {rev = "as/hw-aux-normals", url = "https://github.com/asinghvi17/Hikari.jl.git"}
KernelInterfaces = {rev = "sd/mantle-dev", subdir = "lib/KernelInterfaces", url = "https://github.com/SimonDanisch/Mantle.jl"}
Lava = {rev = "as/refit-preserve-custom-index", url = "https://github.com/asinghvi17/Lava.jl.git"}
Makie = {rev = "sd/lava", subdir = "Makie", url = "https://github.com/MakieOrg/Makie.jl.git"}
Mantle = {rev = "sd/mantle-dev", url = "https://github.com/SimonDanisch/Mantle.jl"}
RayMakie = {rev = "fix/recipe-forwarded-material-color", subdir = "RayMakie", url = "https://github.com/MakieOrg/Makie.jl.git"}
Raycore = {rev = "sd/with-texture", url = "https://github.com/JuliaGeometry/Raycore.jl"}
Vulkan = {rev = "sd/vk1.4", url = "https://github.com/JuliaGPU/Vulkan.jl.git"}
VulkanCore = {rev = "sd/vk1.4", url = "https://github.com/JuliaGPU/VulkanCore.jl.git"}

[compat]
DimensionalData = "0.30"
GeoMakie = "0.7"
Geomorphometry = "0.7"
Makie = "0.24"
julia = "1.11"

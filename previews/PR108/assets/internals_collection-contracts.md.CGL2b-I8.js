import{_ as s,o as t,c as a,ao as i}from"./chunks/framework.B9KUoh5h.js";const g=JSON.parse('{"title":"Collection and traversal contracts","description":"","frontmatter":{},"headers":[],"relativePath":"internals/collection-contracts.md","filePath":"internals/collection-contracts.md","lastUpdated":null}'),n={name:"internals/collection-contracts.md"};function o(r,e,l,c,d,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="Collection-and-traversal-contracts" tabindex="-1">Collection and traversal contracts <a class="header-anchor" href="#Collection-and-traversal-contracts" aria-label="Permalink to &quot;Collection and traversal contracts {#Collection-and-traversal-contracts}&quot;">​</a></h1><p>This page preserves storage and pruning details for implementors. Start with <a href="./../api/selecting-cells">Selecting cells</a> for collection choices and conversions.</p><h2 id="CellVector-storage-and-expansion" tabindex="-1">CellVector storage and expansion <a class="header-anchor" href="#CellVector-storage-and-expansion" aria-label="Permalink to &quot;CellVector storage and expansion {#CellVector-storage-and-expansion}&quot;">​</a></h2><p>CellVector(set::MultiOrderCellSet; level = set&#39;s reference level) CellVector(grid::AbstractGrid) CellVector(sys, level, ids::AbstractVector)</p><p>An immutable, lazy <code>AbstractVector</code> of strictly ascending cell ids from one level of one hierarchical system. It stores their leaf-grid index windows instead of the ids.</p><p>Semantically <code>cv</code> <strong>is</strong> the id vector: <code>length(cv)</code> is the number of cells, <code>cv[k]</code> is the <code>k</code>th of them, <code>collect(cv)</code> is the vector itself. What is <em>stored</em> is sorted, disjoint intervals or a sorted index list in <code>levelgrid(system(cv), level(cv))</code>, so memory is O(number of windows) rather than O(number of cells). <code>cv[k]</code> searches the windows&#39; cumulative lengths and resolves one <code>cellindex</code>; <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/grid-interface#DiscreteGlobalGrids.localindex"><code>localindex</code></a> runs the inverse. Nothing is materialised.</p><p><a href="/DiscreteGlobalGrids.jl/previews/PR108/api/selecting-cells#DiscreteGlobalGrids.CellLookups.CellLookup"><code>CellLookup</code></a> provides the DimensionalData wrapper and delegates its collection operations to this type.</p><h1 id="The-three-ways-in" tabindex="-1">The three ways in <a class="header-anchor" href="#The-three-ways-in" aria-label="Permalink to &quot;The three ways in {#The-three-ways-in}&quot;">​</a></h1><ul><li><p>a <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/selecting-cells#DiscreteGlobalGrids.Engine.MultiOrderCellSet"><code>MultiOrderCellSet</code></a>, the compressed coverage, optionally re-expanded to a deeper <code>level</code> than the set&#39;s own reference level;</p></li><li><p>an <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/grid-interface#DiscreteGlobalGrids.AbstractGrid"><code>AbstractGrid</code></a> — <code>levelgrid(sys, l)</code>, a whole level, which is one window; or a <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/grid-interface#DiscreteGlobalGrids.Engine.PartialGrid"><code>PartialGrid</code></a>, which is that subset&#39;s indices (one window when the subset is a subtree, the explicit list when it is scattered);</p></li><li><p><code>sys, level, ids</code> — an explicit <strong>strictly ascending</strong> id vector, validated and run-compressed.</p></li></ul><p><code>CellVector(cv)</code> returns <code>cv</code> unchanged.</p><h1 id="The-verbs" tabindex="-1">The verbs <a class="header-anchor" href="#The-verbs" aria-label="Permalink to &quot;The verbs {#The-verbs}&quot;">​</a></h1><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cv[k]                          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the kth cell id</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">localindex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv, c)              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># its inverse: Int, or \`nothing\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cv                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># membership, O(log #windows)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">localindex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv, lon, lat)       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the index of the cell a point falls in</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cellat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv, lon, lat)           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># that cell&#39;s id instead</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">covering</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv, polygon)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the sub-vector a region&#39;s coverage names</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">intersect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv, other)           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># two vectors at the same level, O(#windows)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PartialGrid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv)                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># read as a grid, O(1) — the regridding handshake</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cellset</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cv)                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># what it was built from</span></span></code></pre></div><p><code>Base.summarysize(cv)</code> is O(number of windows) plus the object referenced by <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/selecting-cells#DiscreteGlobalGrids.Engine.cellset"><code>cellset</code></a>. Indexing is O(log(number of windows)) and allocation-free.</p><p>Where <a href="/DiscreteGlobalGrids.jl/previews/PR108/internals/grid-contracts#DiscreteGlobalGrids.has_sorted_subtrees"><code>has_sorted_subtrees</code></a> holds, <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/selecting-cells#DiscreteGlobalGrids.Engine.level_ranges"><code>level_ranges</code></a> constructs the windows in O(#entries). Where it does not (A5), a cell&#39;s descendants are not one interval of their level, so the vector is built by explicit selection: <code>descendants</code> names the leaves, they are resolved to indices and sorted, and the result is run-compressed like any other index list. Every method above is unchanged as any other index list. This construction visits every leaf. The stored form ranges from a small set of windows to one index per cell.</p><p>For A5, descendants need not lie inside their parent&#39;s footprint, so expanding a coverage names leaves the target does not touch — most visibly inside a hole. A <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/selecting-cells#DiscreteGlobalGrids.Engine.covering"><code>covering</code></a> subset on A5 is therefore a superset of the cells that meet the region, by the same margin the refinement itself is; see <a href="/DiscreteGlobalGrids.jl/previews/PR108/api/selecting-cells#DiscreteGlobalGrids.Engine.MultiOrderCoverage"><code>MultiOrderCoverage</code></a>.</p><h2 id="Subtree-extent-contract" tabindex="-1">Subtree extent contract <a class="header-anchor" href="#Subtree-extent-contract" aria-label="Permalink to &quot;Subtree extent contract {#Subtree-extent-contract}&quot;">​</a></h2><p>node_extent(sys::AbstractHierarchicalGridSystem, c::AbstractCellIndex) -&gt; GO.UnitSpherical.SphericalCap</p><p>The covering region of the entire subtree rooted at <code>c</code>.</p><p><strong>Defaulted</strong>: the generic implementation inflates the cell&#39;s own bounding cap by <a href="/DiscreteGlobalGrids.jl/previews/PR108/internals/grid-contracts#DiscreteGlobalGrids.cap_inflation"><code>cap_inflation(sys)</code></a>, and a system able to compute a tighter covering cap overrides it. The covering law below holds either way, and validating it is the system&#39;s responsibility.</p><blockquote><p><strong>Guaranteed.</strong> <code>node_extent(sys, c)</code> contains the <em>geometry</em> of every descendant of <code>c</code>, at every depth — every point of every cell boundary in the subtree, all the way down to <code>maxlevel(sys)</code>, independent of a caller&#39;s planned traversal depth.</p><p><strong>Not guaranteed.</strong> It need not contain the descendants&#39; <code>cell_cap</code>s or their own <code>node_extent</code>s, and for real systems it does not. A cap is an inflated or sampled bound <em>around</em> geometry, not geometry, so a child&#39;s cap can stand partly outside its parent&#39;s extent while every child polygon lies inside it.</p></blockquote><p>Both halves are load-bearing. A cell&#39;s own boundary does not cover its subtree — aperture-7 children extend beyond the parent boundary — so <code>cell_cap</code> is never a substitute for <code>node_extent</code>. Equally, the extents down a branch are not a nested chain of caps: a consumer that compares a parent&#39;s <code>node_extent</code> against a child&#39;s <code>node_extent</code> or <code>cell_cap</code>, or that expects radii to shrink with depth, is testing a property this function does not have and never promised.</p><p>Pruning is sound against queries derived from geometry, which is what tree descent asks: a query cap disjoint from <code>node_extent(sys, c)</code> meets no descendant cell polygon, so discarding the subtree can drop no result. Every node is tested against the query, never against its parent. Over-coverage only reduces pruning; under-coverage can omit valid results. For a convex cap of angular radius at most 90°, containing all boundary vertices also contains their great-circle arcs. Non-convex extents must establish containment of the full geometry.</p><p>A merge of the children&#39;s caps (<code>Extents.union</code> over <code>cell_cap</code>) is a bound over caps — a different and looser object, and one that says nothing about levels below those children. It is not <code>node_extent</code> and does not substitute for it.</p><p>Extents are <code>SphericalCap</code>s throughout this package, at every node of every tree, which is what lets one predicate vocabulary serve all of them.</p>`,24)])])}const v=s(n,[["render",o]]);export{g as __pageData,v as default};

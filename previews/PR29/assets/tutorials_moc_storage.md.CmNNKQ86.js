import{_ as a,o as e,c as n,ap as i}from"./chunks/framework.CztdyVBW.js";const k=JSON.parse('{"title":"Multi-order storage","description":"","frontmatter":{},"headers":[],"relativePath":"tutorials/moc_storage.md","filePath":"tutorials/moc_storage.md","lastUpdated":null}'),l={name:"tutorials/moc_storage.md"};function t(p,s,o,r,h,d){return e(),n("div",null,[...s[0]||(s[0]=[i(`<h1 id="Multi-order-storage" tabindex="-1">Multi-order storage <a class="header-anchor" href="#Multi-order-storage" aria-label="Permalink to &quot;Multi-order storage {#Multi-order-storage}&quot;">​</a></h1><p>Astronomers store sky regions as <strong>Multi-Order Coverage</strong> maps: HEALPix cells at mixed orders, coarse inside, fine along the edge. The multi-order coverage page builds that as a <em>region</em>; this page attaches <em>data</em> — one value per cell, at mixed levels, as in adaptive mesh refinement.</p><p>A smooth field needs leaf cells only where it varies, so a mixed-level container can hold it to a stated tolerance in a fraction of the leaf count and still answer every leaf query.</p><p>The plan: sample a real field onto a full HEALPix level, coarsen it adaptively, then read the result back at full resolution.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ENV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;RASTERDATASOURCES_PATH&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> mkpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ENV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;RASTERDATASOURCES_PATH&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    joinpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">tempdir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rasterdatasources&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)))</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DiscreteGlobalGrids </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DGG</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DimensionalData </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DD</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GeometryOps </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GO</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GeoInterface </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GI</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Rasters, RasterDataSources</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ArchGDAL</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Statistics</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GLMakie, GeoMakie</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GLMakie</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">activate!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(inline </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="The-source" tabindex="-1">The source <a class="header-anchor" href="#The-source" aria-label="Permalink to &quot;The source {#The-source}&quot;">​</a></h2><p>WorldClim&#39;s July mean temperature at 10 arc-minutes: one global raster in °C over land, <code>missing</code> over the ocean. RasterDataSources downloads it once and caches it under <code>RASTERDATASOURCES_PATH</code>.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>raster = Raster(WorldClim{Climate}, :tavg; month = 7, res = &quot;10m&quot;)</span></span>
<span class="line"><span>size(raster)</span></span></code></pre></div><h2 id="Choosing-the-destination-level" tabindex="-1">Choosing the destination level <a class="header-anchor" href="#Choosing-the-destination-level" aria-label="Permalink to &quot;Choosing the destination level {#Choosing-the-destination-level}&quot;">​</a></h2><p>A lon/lat pixel is widest at the equator, so the equatorial pixel is the coarsest the source gets. HEALPix cells are equal-area (<code>cell_area</code> is <code>4π / (12 * 4^level)</code> steradians); &quot;across&quot; below is √area, as on the other pages.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>R = 6371.0088                            # mean Earth radius, km</span></span>
<span class="line"><span>pixel_km = (10 / 60) * (2pi * R / 360)   # a 10 arc-minute pixel at the equator</span></span>
<span class="line"><span>println(&quot;raster: &quot;, length(raster), &quot; pixels, &quot;, round(pixel_km; digits = 1),</span></span>
<span class="line"><span>        &quot; km wide at the equator&quot;)</span></span>
<span class="line"><span>for l in 6:9</span></span>
<span class="line"><span>    g = DGG.levelgrid(DGG.HEALPixSystem(), l)</span></span>
<span class="line"><span>    across = sqrt(DGG.cell_area(g, DGG.CellVector(g)[1]) * R^2)</span></span>
<span class="line"><span>    println(&quot;level &quot;, l, &quot;: &quot;, lpad(DGG.ncells(g), 8), &quot; cells, &quot;,</span></span>
<span class="line"><span>            lpad(round(across; digits = 1), 6), &quot; km across, &quot;,</span></span>
<span class="line"><span>            lpad(round(across / pixel_km; digits = 2), 5), &quot; × that pixel&quot;)</span></span>
<span class="line"><span>end</span></span></code></pre></div><p>Level 8 is the finest level whose cells are still wider than the equatorial pixel, so sampling onto it cannot invent detail, and its leaf array is under a million values.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">L </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">grid </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DGG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">levelgrid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(DGG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">HEALPixSystem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), L)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cells </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DGG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CellVector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(grid)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">CellVector(HEALPixSystem, level=8, ncells=786432, 1 windows)</span></span></code></pre></div><p><code>CellVector(grid)</code> is the whole level as a lazy vector of cell ids, one per position — on HEALPix, the nested order astronomy ships all-sky maps in.</p><h2 id="Sampling-onto-the-cells" tabindex="-1">Sampling onto the cells <a class="header-anchor" href="#Sampling-onto-the-cells" aria-label="Permalink to &quot;Sampling onto the cells {#Sampling-onto-the-cells}&quot;">​</a></h2><p>One value per cell, taken at the cell centroid by nearest-neighbour lookup into the raster. The regridding page has the conservative, area-exact alternative; everything below works the same either way.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tolonlat = GO.UnitSpherical.GeographicFromUnitSphere()</span></span>
<span class="line"><span>centers = [tolonlat(DGG.cell_centroid(grid, c)) for c in cells]</span></span>
<span class="line"><span>tavg = [raster[X(Near(lon)), Y(Near(lat))] for (lon, lat) in centers]</span></span></code></pre></div><p><code>Near</code> propagates the raster&#39;s <code>missing</code>s, so ocean cells arrive as <code>missing</code> rather than as a sentinel.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>(; cells = length(tavg), land = count(!ismissing, tavg),</span></span>
<span class="line"><span>   sea = count(ismissing, tavg))</span></span></code></pre></div><h2 id="The-leaf-array" tabindex="-1">The leaf array <a class="header-anchor" href="#The-leaf-array" aria-label="Permalink to &quot;The leaf array {#The-leaf-array}&quot;">​</a></h2><p><code>CellLookup(grid)</code> reads a whole level as a cell axis, <code>Cells</code> is the DimensionalData dimension, and the result is an ordinary <code>DimArray</code>.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A = DD.DimArray(Vector{Union{Float64, Missing}}(tavg), DGG.Cells(DGG.CellLookup(grid)))</span></span></code></pre></div><p>It indexes by position like any vector, and by point through the axis. (<code>DD.Contains</code> is DimensionalData&#39;s point selector, reached through <code>DD</code> because this package exports DE9IM&#39;s <code>Contains</code>, a different thing.)</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A[DGG.Cells(DD.Contains((2.35, 48.86)))]  # July mean over Paris</span></span></code></pre></div><p>Equal-area cells make the plain mean over land cells the area-weighted land mean.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>(; land_mean = mean(skipmissing(A)), range = extrema(skipmissing(A)))</span></span></code></pre></div><h2 id="The-leaf-data,-drawn" tabindex="-1">The leaf data, drawn <a class="header-anchor" href="#The-leaf-data,-drawn" aria-label="Permalink to &quot;The leaf data, drawn {#The-leaf-data,-drawn}&quot;">​</a></h2><p><code>cell_polygon</code> gives each cell&#39;s unit-sphere polygon and <code>GO.transform</code> takes the vector to lon/lat. Cells straddling the antimeridian would paint full-width bands in planar longitude, so they are dropped from the figure.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">polys </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GO</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">transform</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(GO</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GeographicFromUnitSphere</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), DGG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cell_polygon</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Ref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(grid), cells))</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lonspan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ring </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GI</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">coordinates</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p)[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">maximum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(first, ring) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> minimum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(first, ring))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">onmap </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> findall</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lonspan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 180</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, polys)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cells) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(onmap)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">1265</span></span></code></pre></div><p>The colour scale clips at ±40 °C so midwinter Antarctica does not flatten the rest of the range; ocean cells are <code>missing</code>, drawn in gray. The colour vector is built over <code>parent(A)</code>: a comprehension over the <code>DimArray</code> returns a <code>DimArray</code>, which Makie would read as a per-vertex colouring.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>colors = [ismissing(v) ? NaN : v for v in parent(A)]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>fig = Figure(size = (900, 470))</span></span>
<span class="line"><span>ax = GeoAxis(fig[1, 1]; dest = &quot;+proj=moll&quot;,</span></span>
<span class="line"><span>    title = &quot;WorldClim July mean temperature, HEALPix level $L&quot;)</span></span>
<span class="line"><span>plt = poly!(ax, polys[onmap]; color = colors[onmap], colormap = Reverse(:RdYlBu),</span></span>
<span class="line"><span>    colorrange = (-40, 40), highclip = :darkred, lowclip = :midnightblue,</span></span>
<span class="line"><span>    nan_color = :gray85, strokewidth = 0)</span></span>
<span class="line"><span>Colorbar(fig[1, 2], plt; label = &quot;mean temperature (°C)&quot;)</span></span>
<span class="line"><span>fig</span></span></code></pre></div><h2 id="A-fixed-level-pyramid" tabindex="-1">A fixed-level pyramid <a class="header-anchor" href="#A-fixed-level-pyramid" aria-label="Permalink to &quot;A fixed-level pyramid {#A-fixed-level-pyramid}&quot;">​</a></h2><p><code>aggregate(f, A, l)</code> gives one value per level-<code>l</code> ancestor of the array&#39;s cells, <code>f</code> seeing the leaf values beneath it; one call per level builds a pyramid.</p><p>The reducer must define the all-ocean case: <code>mean</code> over a group holding any <code>missing</code> answers <code>missing</code>, and <code>mean(skipmissing(v))</code> over an all-<code>missing</code> group answers <code>NaN</code>. Neither means &quot;no land here&quot;, so the reducer says it explicitly.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>landmean(v) = count(!ismissing, v) == 0 ? missing : mean(skipmissing(v))</span></span>
<span class="line"><span></span></span>
<span class="line"><span>pyramid = [DGG.aggregate(landmean, A, l) for l in (6, 5, 4)]</span></span>
<span class="line"><span>[(; level = DGG.level(DD.lookup(P, DGG.Cells)), cells = length(P),</span></span>
<span class="line"><span>    land = count(!ismissing, parent(P))) for P in pyramid]</span></span></code></pre></div><p>Polygons and the antimeridian drop are recomputed per cell set — a coarse cell can straddle the line where none of its children did.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onmapof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ps) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> findall</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lonspan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 180</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ps)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cellpolys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(lk) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GO</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">transform</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(GO</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GeographicFromUnitSphere</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">                             collect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(DGG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">getcell</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(DGG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PartialGrid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(lk))))</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">cellpolys (generic function with 1 method)</span></span></code></pre></div><p>One cell&#39;s width in kilometres at its own level — the √area convention again, now that levels are mixed.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>across_km(c) = sqrt(DGG.cell_area(DGG.levelgrid(DGG.HEALPixSystem(), DGG.level(c)), c) * R^2)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>fig = Figure(size = (1000, 300))</span></span>
<span class="line"><span>for (k, P) in enumerate(pyramid)</span></span>
<span class="line"><span>    local lk = DD.lookup(P, DGG.Cells)</span></span>
<span class="line"><span>    local across = across_km(lk[1])</span></span>
<span class="line"><span>    local ps = cellpolys(lk)</span></span>
<span class="line"><span>    local keep = onmapof(ps)</span></span>
<span class="line"><span>    local panel = GeoAxis(fig[1, k]; dest = &quot;+proj=moll&quot;,</span></span>
<span class="line"><span>        title = &quot;level $(DGG.level(lk)), $(round(Int, across)) km across&quot;)</span></span>
<span class="line"><span>    poly!(panel, ps[keep];</span></span>
<span class="line"><span>        color = [ismissing(v) ? NaN : v for v in parent(P)][keep],</span></span>
<span class="line"><span>        colormap = Reverse(:RdYlBu), colorrange = (-40, 40),</span></span>
<span class="line"><span>        highclip = :darkred, lowclip = :midnightblue, nan_color = :gray85,</span></span>
<span class="line"><span>        strokewidth = 0)</span></span>
<span class="line"><span>end</span></span>
<span class="line"><span>fig</span></span></code></pre></div><p>A fixed level stores a value for every cell of that level, ocean included, and applies one resolution to the whole globe. A spread reducer shows what that costs:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>worst(v) = (m = collect(skipmissing(v)); isempty(m) ? missing : maximum(m) - minimum(m))</span></span>
<span class="line"><span>for l in 7:-1:4</span></span>
<span class="line"><span>    S = DGG.aggregate(worst, A, l)</span></span>
<span class="line"><span>    println(&quot;level &quot;, l, &quot;: worst cell replaces a spread of &quot;,</span></span>
<span class="line"><span>            round(maximum(skipmissing(parent(S))); digits = 1), &quot; °C&quot;)</span></span>
<span class="line"><span>end</span></span></code></pre></div><p>A level-6 cell on the Antarctic coast, 100 km across, replaces a spread of tens of degrees; one in the Sahara replaces a fraction of a degree. A fixed level cannot treat the two differently.</p><h2 id="Adaptive-desampling" tabindex="-1">Adaptive desampling <a class="header-anchor" href="#Adaptive-desampling" aria-label="Permalink to &quot;Adaptive desampling {#Adaptive-desampling}&quot;">​</a></h2><p><code>coarsen</code> merges a sibling group when the group is complete in the array and its leaf values span at most <code>atol</code>, or when every value is <code>missing</code>. A group mixing <code>missing</code> with data never merges, so coastline cells are never averaged into the sea. The criterion is monotone, so each leaf ends up under the coarsest cell that satisfies it.</p><p><code>atol</code> is the caller&#39;s choice; a sweep shows what each costs:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for atol in (0.25, 0.5, 1.0, 2.0, 4.0)</span></span>
<span class="line"><span>    Mt = DGG.coarsen(A; atol)</span></span>
<span class="line"><span>    println(&quot;atol = &quot;, rpad(atol, 4), &quot; °C: &quot;, lpad(length(Mt), 6), &quot; cells stored, &quot;,</span></span>
<span class="line"><span>            lpad(round(100 * length(Mt) / length(A); digits = 1), 4), &quot;% of the leaf array&quot;)</span></span>
<span class="line"><span>end</span></span></code></pre></div><p>One degree is the granularity a monthly climatology is read at, and far below the field&#39;s range of over a hundred degrees; the rest of the page uses it.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>M = DGG.coarsen(A; atol = 1.0)</span></span>
<span class="line"><span>mov = parent(DD.lookup(M, DGG.Cells))</span></span></code></pre></div><p>The result is a <code>DimArray</code> over a <code>MultiOrderLookup</code>: one value per stored cell, each at whatever level merging stopped. The lookup wraps a <code>MultiOrderVector</code> — <code>mov</code> above — a plain vector of cells usable with no datacube library; the lookup only makes it an axis. The rest of the page passes <code>mov</code> directly to <code>cell_polygons</code>, <code>cellat</code> and <code>summarysize</code>.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>levs = DGG.level.(mov)</span></span>
<span class="line"><span>for l in sort(unique(levs))</span></span>
<span class="line"><span>    at = findall(==(l), levs)</span></span>
<span class="line"><span>    println(&quot;level &quot;, l, &quot;: &quot;, lpad(length(at), 7), &quot; cells, &quot;,</span></span>
<span class="line"><span>            lpad(round(Int, across_km(mov[first(at)])), 5), &quot; km across, &quot;,</span></span>
<span class="line"><span>            lpad(count(ismissing, parent(M)[at]), 6), &quot; ocean, &quot;,</span></span>
<span class="line"><span>            lpad(count(!ismissing, parent(M)[at]), 6), &quot; land&quot;)</span></span>
<span class="line"><span>end</span></span></code></pre></div><p><code>minlevel</code> defaults to the shallowest level the system has; merging reached level 2 on its own, on 34 open-ocean cells 1600 km across. Almost everything coarser than level 6 is ocean; the one land cell at level 4 is in the western Amazon, where the July mean is flat over 165,000 km².</p><p>Drawn twice: by value, which should match the leaf map, and by cell level, which is the mesh itself.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>mpolys = GO.transform(GO.GeographicFromUnitSphere(), DGG.cell_polygons(mov))</span></span>
<span class="line"><span>mkeep = onmapof(mpolys)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>fig = Figure(size = (1000, 340))</span></span>
<span class="line"><span>axv = GeoAxis(fig[1, 1]; dest = &quot;+proj=moll&quot;, title = &quot;value, atol = 1 °C&quot;)</span></span>
<span class="line"><span>pv = poly!(axv, mpolys[mkeep];</span></span>
<span class="line"><span>    color = [ismissing(v) ? NaN : v for v in parent(M)][mkeep],</span></span>
<span class="line"><span>    colormap = Reverse(:RdYlBu), colorrange = (-40, 40),</span></span>
<span class="line"><span>    highclip = :darkred, lowclip = :midnightblue, nan_color = :gray85, strokewidth = 0)</span></span>
<span class="line"><span>Colorbar(fig[2, 1], pv; vertical = false, label = &quot;mean temperature (°C)&quot;)</span></span>
<span class="line"><span>axl = GeoAxis(fig[1, 2]; dest = &quot;+proj=moll&quot;, title = &quot;cell level&quot;)</span></span>
<span class="line"><span>pl = poly!(axl, mpolys[mkeep]; color = levs[mkeep],</span></span>
<span class="line"><span>    colormap = :isoluminant_cgo_70_c39_n256, colorrange = extrema(levs), strokewidth = 0)</span></span>
<span class="line"><span>Colorbar(fig[2, 2], pl; vertical = false, ticks = extrema(levs)[1]:extrema(levs)[2])</span></span>
<span class="line"><span>fig</span></span></code></pre></div><p>The ocean is a few huge cells, flat interiors are large ones, and the leaf level survives along coastlines, mountain ranges and the Antarctic margin. By value, the field is unchanged to the eye.</p><p>The cost in cells and bytes:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>(; stored = length(M), leaves = length(A),</span></span>
<span class="line"><span>   values_MiB = round(Base.summarysize(parent(M)) / 2^20; digits = 2),</span></span>
<span class="line"><span>   mesh_MiB = round(Base.summarysize(mov) / 2^20; digits = 2),</span></span>
<span class="line"><span>   dense_MiB = round(Base.summarysize(parent(A)) / 2^20; digits = 2))</span></span></code></pre></div><p>The values shrink by the cell ratio, a little over five times. The index does not: a cell id and three interval bounds per stored cell — some forty bytes — gives most of that saving back at this tolerance.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>round(Int, Base.summarysize(mov) / length(M))  # bytes of index per stored cell</span></span></code></pre></div><h2 id="Reading-it-back" tabindex="-1">Reading it back <a class="header-anchor" href="#Reading-it-back" aria-label="Permalink to &quot;Reading it back {#Reading-it-back}&quot;">​</a></h2><p><code>Contains</code> answers a point with the stored cell that covers it, at whatever level that cell is. <code>At</code> selects an exact cell and throws if it is absent.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for (name, lon, lat) in [(&quot;Lisbon&quot;, -9.14, 38.72), (&quot;Denver&quot;, -104.99, 39.74),</span></span>
<span class="line"><span>                         (&quot;central Sahara&quot;, 15.0, 24.0), (&quot;Point Nemo&quot;, -123.4, -48.9)]</span></span>
<span class="line"><span>    v = M[DGG.Cells(DD.Contains((lon, lat)))]</span></span>
<span class="line"><span>    c = DGG.cellat(mov, lon, lat)</span></span>
<span class="line"><span>    across = sqrt(DGG.cell_area(DGG.levelgrid(DGG.HEALPixSystem(), DGG.level(c)), c) * R^2)</span></span>
<span class="line"><span>    println(rpad(name, 15), ismissing(v) ? &quot;     missing&quot; : lpad(round(v; digits = 1), 9) * &quot; °C&quot;,</span></span>
<span class="line"><span>            &quot;  from a level-&quot;, DGG.level(c), &quot; cell, &quot;, round(Int, across), &quot; km across&quot;)</span></span>
<span class="line"><span>end</span></span></code></pre></div><p>Lisbon, on a coastline, answers from a leaf cell; the Sahara and the Rockies answer from a cell four times the area; the South Pacific answers <code>missing</code> from a cell 1600 km across.</p><p><code>expand</code> presents the whole array at one level again:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>E = DGG.expand(M, L)</span></span>
<span class="line"><span>DD.lookup(E, DGG.Cells) == DD.lookup(A, DGG.Cells)</span></span></code></pre></div><p>The same axis as <code>A</code>, cell for cell. The storage is unchanged — one value per stored cell plus a leaf count each, indexed by binary search — so the presentation level moves while the memory does not.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for l in (L, L + 1, L + 2, L + 4)</span></span>
<span class="line"><span>    El = DGG.expand(M, l)</span></span>
<span class="line"><span>    dense = Base.summarysize(parent(A)) / length(A) * length(El)</span></span>
<span class="line"><span>    println(&quot;presented at level &quot;, l, &quot;: &quot;, lpad(length(El), 10), &quot; cells, &quot;,</span></span>
<span class="line"><span>            lpad(round(Base.summarysize(parent(El)) / 2^20; digits = 2), 6),</span></span>
<span class="line"><span>            &quot; MiB stored, &quot;, lpad(round(dense / 2^20; digits = 1), 8), &quot; MiB if dense&quot;)</span></span>
<span class="line"><span>end</span></span></code></pre></div><p>At the leaf level the expanded array is already smaller than the dense one; four levels down it presents two hundred million cells — over a gigabyte if dense — from the same stored bytes.</p><p><code>coarsen</code>&#39;s criterion bounds the error: a stored value lies between the minimum and maximum of the leaves it replaces, which differ by at most <code>atol</code>. The leaf-by-leaf check:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>err = abs.(collect(parent(E)) .- parent(A))</span></span>
<span class="line"><span>(; atol = 1.0, max_error = maximum(skipmissing(err)),</span></span>
<span class="line"><span>   mean_error = mean(skipmissing(err)),</span></span>
<span class="line"><span>   unchanged = count(iszero, skipmissing(err)),</span></span>
<span class="line"><span>   missing_agrees = all(ismissing.(collect(parent(E))) .== ismissing.(parent(A))))</span></span></code></pre></div><p><code>missing</code> round-trips exactly: an all-<code>missing</code> group merges to <code>missing</code> and a mixed group never merges. Two fifths of the land cells are bit-identical — the ones kept at the leaf level. Mapping the rest, one worst case per level-6 cell:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>worsterr = DGG.aggregate(v -&gt; (m = collect(skipmissing(v)); isempty(m) ? missing : maximum(m)),</span></span>
<span class="line"><span>                         DD.DimArray(err, DGG.Cells(DGG.CellLookup(grid))), 6)</span></span>
<span class="line"><span>epolys = cellpolys(DD.lookup(worsterr, DGG.Cells))</span></span>
<span class="line"><span>ekeep = onmapof(epolys)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>fig = Figure(size = (900, 450))</span></span>
<span class="line"><span>ax = GeoAxis(fig[1, 1]; dest = &quot;+proj=moll&quot;,</span></span>
<span class="line"><span>    title = &quot;worst leaf error per 100 km cell, atol = 1 °C&quot;)</span></span>
<span class="line"><span>plt = poly!(ax, epolys[ekeep];</span></span>
<span class="line"><span>    color = [ismissing(v) ? NaN : v for v in parent(worsterr)][ekeep],</span></span>
<span class="line"><span>    colormap = :magma, colorrange = (0, 1.0), nan_color = :gray85, strokewidth = 0)</span></span>
<span class="line"><span>Colorbar(fig[1, 2], plt; label = &quot;|expanded − leaf| (°C)&quot;)</span></span>
<span class="line"><span>fig</span></span></code></pre></div><p>The error is largest where the field has structure at the merge scale — the Andes, the Himalayan front, the Rift, the Antarctic margin. The black zeros are cells kept at the leaf level: unmerged cells are copied, not approximated.</p><h2 id="Summary" tabindex="-1">Summary <a class="header-anchor" href="#Summary" aria-label="Permalink to &quot;Summary {#Summary}&quot;">​</a></h2><p><code>coarsen</code> picks a level per cell from the data and a tolerance; the axis answers points and regions on the mixed levels directly; <code>expand</code> presents the result at any level without materialising it. Storage scales with stored cells and resolution with leaves: the same two megabytes present 786,432 cells or two hundred million.</p><p>The multi-order coverage page builds the same structure as a <em>region</em> — the query side. The regridding page replaces this page&#39;s nearest-neighbour sampling with conservative, area-exact regridding.</p><hr><p><em>This page was generated using <a href="https://github.com/fredrikekre/Literate.jl" target="_blank" rel="noreferrer">Literate.jl</a>.</em></p>`,80)])])}const g=a(l,[["render",t]]);export{k as __pageData,g as default};

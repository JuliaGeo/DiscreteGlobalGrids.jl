import{_ as t,o as a,c as o,ao as s}from"./chunks/framework.CHDKlDff.js";const u=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md","lastUpdated":null}'),i={name:"architecture.md"};function n(r,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="Architecture" tabindex="-1">Architecture <a class="header-anchor" href="#Architecture" aria-label="Permalink to &quot;Architecture {#Architecture}&quot;">​</a></h1><p>This page describes the abstractions in DiscreteGlobalGrids: what each one is, what it demands of an implementor, and why it exists. It is written for someone who has not read the source.</p><p>A discrete global grid system tessellates the sphere into cells, usually recursively, so that every cell has an identifier and a geometry. The package implements seven of them and makes them answerable through one set of verbs, without giving up the closed-form arithmetic each one has of its own.</p><h2 id="Grids-and-systems" tabindex="-1">Grids and systems <a class="header-anchor" href="#Grids-and-systems" aria-label="Permalink to &quot;Grids and systems {#Grids-and-systems}&quot;">​</a></h2><p>The two root types are disjoint: neither is a subtype of the other.</p><p>An <code>AbstractGrid</code> is a finite, concrete collection of cells. It has a length, and index <code>i</code> means a local index — a place in <code>1:ncells(grid)</code>. An <code>AbstractHierarchicalGridSystem</code> is a rule for tessellating the sphere, with analytic parent and child structure. It is not a collection and has no length.</p><p>The split is what lets the package accept grids that no hierarchy produced — a tripolar ocean grid, a cubed sphere, anything whose neighbours must be derived geometrically. Such a grid supplies four methods and gets tree building, point location, adjacency, and geometry from generic fallbacks. A grid that <em>is</em> backed by a system keeps those fallbacks as a floor and overrides whatever it can do faster.</p><p>A grid must implement exactly four things: <code>ncells</code>, <code>cellindex</code>, <code>cell_boundary</code>, and <code>cell_centroid</code>. Everything else in the grid interface has a working default. A system must implement <code>cellindextype</code>, <code>levels</code>, <code>rootcells</code>, <code>parent</code>, <code>children</code>, and five level-grid primitives described below.</p><h2 id="Level-grids" tabindex="-1">Level grids <a class="header-anchor" href="#Level-grids" aria-label="Permalink to &quot;Level grids {#Level-grids}&quot;">​</a></h2><p>No shipped system defines a grid type of its own. <code>levelgrid(sys, l)</code> returns a <code>HierarchicalLevelGrid</code>, which holds nothing but the system and the level, and forwards the four grid primitives to five system-level ones: <code>ncells(sys, l)</code>, <code>cellindex(sys, l, i)</code>, <code>globalindex(sys, c)</code>, <code>cell_boundary(sys, c)</code>, and <code>cell_centroid(sys, c)</code>.</p><p>There are five rather than four because a complete level needs a real <code>globalindex</code>. The generic grid version is a linear scan over every cell, which is fine for an exotic grid of a few thousand cells and useless for a level of a hierarchical system.</p><p>Everything beyond those four — <code>cellat</code>, <code>neighbors</code>, <code>ring</code>, <code>cell_area</code> — a system attaches to <code>HierarchicalLevelGrid{ItsOwnSystem}</code>. So the fast paths dispatch on the type parameter, grid construction stays free, and the package needs only three concrete grid types in total: the level grid, <code>PartialGrid</code>, and the authalic wrapper.</p><h2 id="Cell-indexing" tabindex="-1">Cell indexing <a class="header-anchor" href="#Cell-indexing" aria-label="Permalink to &quot;Cell indexing {#Cell-indexing}&quot;">​</a></h2><p>Many grid systems define their own identifier scheme; some define several; some define none. Computing an identifier, or going from an identifier back to an index in an array, can be expensive.</p><p>So there are two ways to name a cell, and both are always available. A bare <code>Int</code> is always an <strong>index</strong> in <code>1:ncells(grid)</code> — a local index into that collection&#39;s own storage. An <code>AbstractCellIndex</code> is a typed identity that records its own level. There is no wrapper type for indices — the two are separated by method signature alone.</p><p>A cell index must be an immutable isbits struct with a total <code>Base.isless</code> giving canonical order, plus <code>level</code> and <code>rawid</code>. Equality and hashing are never defined for any of the five concrete index types; they come free from Julia&#39;s structural equality on isbits values, which is why immutability is load-bearing rather than stylistic.</p><p>The single gate between the two worlds is internal: a cell of the wrong level, or of a scheme the system does not claim, resolves to <code>nothing</code> rather than throwing. That is what lets <code>globalindex</code> be asked safely about any cell from anywhere.</p><p><code>reindex</code> converts a cell between alternate schemes of one system. Only HEALPix has more than one — nested and ring — so every other system gets identity-or-throw.</p><h2 id="The-quad-face-family" tabindex="-1">The quad-face family <a class="header-anchor" href="#The-quad-face-family" aria-label="Permalink to &quot;The quad-face family {#The-quad-face-family}&quot;">​</a></h2><p><code>AbstractQuadFaceGridSystem</code> covers systems whose identifier is <code>face * 4^level + curvecode</code> over some number of congruent square lattices. HEALPix, S2 and ISEA4R belong to it.</p><p>A member declares three things — how many base faces it has, and two names used in error messages — and inherits parent, children, ancestor, descendant ranges, descendants, cell counts, identifier and index arithmetic, sorted-subtree status, the Morton codec, and the border and interior walkers. All of it is bit arithmetic on the identifier. In practice a new member of this family needs six methods: the three declarations plus <code>levels</code>, <code>cell_boundary</code> and <code>cell_centroid</code>.</p><p>This is the largest piece of reuse in the package. For comparison, IGeo7, H3 and A5 are not in the family and each writes its own identity and hierarchy layer, between twenty and thirty methods.</p><p>There is no chart abstraction. The three chart files share a naming convention and two helper calls, but no abstract type and no shared interface. They are independent projections that happen to be organised the same way.</p><h2 id="Neighbours" tabindex="-1">Neighbours <a class="header-anchor" href="#Neighbours" aria-label="Permalink to &quot;Neighbours {#Neighbours}&quot;">​</a></h2><p>The hinge of the adjacency layer is <code>one_ring</code>, which returns a cell&#39;s immediate neighbours. It is not exported, but every system implements it, and <code>neighbors</code> and <code>ring</code> are built on top. A grid without a system gets a geometric fallback that finds neighbours by matching shared vertices against a spatial tree.</p><p>Adjacency is not one relation, so <code>Connectivity</code> is an explicit argument rather than a property of the grid. <code>Vertex()</code> and <code>Edge()</code> can differ sharply: A5 has up to eleven vertex-neighbours and five edge-neighbours.</p><p><code>Winding</code> declares whether a system&#39;s ring order is a rotation that can be carried outward to shells at distance two and beyond, or whether those shells must be re-sorted by measured azimuth. Declaring a winding is a speed decision, not a correctness one — the geometric sort is always available and always correct.</p><p><code>maxneighbors</code> is an upper bound, not a count. Declaring one lets the neighbourhood machinery use a fixed-capacity stack container instead of a heap allocation, which is why it is worth declaring even where the bound is loose.</p><h2 id="Regions" tabindex="-1">Regions <a class="header-anchor" href="#Regions" aria-label="Permalink to &quot;Regions {#Regions}&quot;">​</a></h2><p>A region is a subset of one complete level, or a complete level itself. <code>PartialGrid</code>, any <code>AbstractCellVector</code> — computed or read from a store — and a complete grid are all regions; <code>subtree</code> is the verb that turns a subtree into one.</p><p>On a region, <code>neighbors</code> and <code>ring</code> answer the complete level&#39;s question clipped to membership. Distance is measured in the system, not in the subset, so a hole does not lengthen a path around itself.</p><p>Four verbs answer about the region as a whole. <code>halo</code> walks what is outside it, including cells punched out of its middle. <code>border</code> and <code>interior</code> split what is inside. <code>adjacency</code> tables every one-ring at once.</p><p>The first three are lazy iterators, serial, and use memory proportional to tree depth rather than to the answer. <code>adjacency</code> is the opposite: materialised, threaded, and the only one that keeps its halo. It is a CSR table with three row shapes — out-of-region members dropped, marked in place with a zero, or addressed into a <code>[region; halo]</code> buffer. The two full-width shapes preserve slot index against the canonical ring, so a slot number is a direction; the clipped shape preserves order only.</p><p><code>halo</code> selects among several engines: a width-one band walk for the quad-face systems, a seeded boundary automaton for the hexagonal ones, and a generic outside-first hierarchy walk that every specialisation falls back to and that a newly registered system inherits.</p><h2 id="Cell-vectors" tabindex="-1">Cell vectors <a class="header-anchor" href="#Cell-vectors" aria-label="Permalink to &quot;Cell vectors {#Cell-vectors}&quot;">​</a></h2><p><code>AbstractCellVector</code> is the region contract in vector form, and what the four verbs, the neighbourhood sweeps, regridding and plotting are all written against. Five methods are required of it: <code>system</code>, <code>level</code>, <code>size</code>, <code>getindex</code>, and <code>localindex</code>. Two backings ship, and they differ in where element <code>k</code> comes from, not in what it means.</p><p><code>CellVector</code> is the computed one, and the container the rest normalise to. It stores runs of consecutive <em>indices</em> in the complete level grid rather than lists of identifiers, in one of two forms chosen by whichever is smaller: a run-length form, or explicit indices.</p><p>A complete level is a single run. A subtree of a system with sorted subtrees is also a single run, computed without touching a cell. Set operations walk intervals rather than cells. Indexing is a binary search over runs and allocates nothing.</p><p><code>ChunkedCellVector</code> is the stored one — see <em>Store IO</em>. <code>region</code> is the verb that turns any cell vector into the compressed one; it is the identity on <code>CellVector</code> and a memoised conversion on the stored twin, so the region machinery has exactly one implementation of each walk.</p><p><code>PartialGrid</code> is the grid-shaped sibling: a sorted identifier vector plus the complete level grid it forwards geometry to. It is a grid, so data can be laid out against its local indices, and it is a region, so the four verbs accept it.</p><h2 id="Mixed-level-sets" tabindex="-1">Mixed-level sets <a class="header-anchor" href="#Mixed-level-sets" aria-label="Permalink to &quot;Mixed-level sets {#Mixed-level-sets}&quot;">​</a></h2><p><code>MultiOrderCellSet</code> holds cells at different levels — the result of an adaptive query that stopped refining where a coarse cell already sufficed.</p><p>It needs its own type for two reasons. There is no single index space across levels, so it stores sort keys instead of indices. And it deliberately has no <code>halo</code>, <code>border</code>, <code>interior</code> or <code>adjacency</code>, because those questions have no meaning without one level to ask them at; <code>member_neighbors</code> is the cross-level substitute.</p><p>It also carries a per-cell flag recording whether containment was <em>proven</em> or merely not asked. The flag is asymmetric on purpose: the exact containment test costs roughly eighty times what an intersection test costs, so the traversal skips it wherever the answer would not change the result.</p><h2 id="The-cube-layer" tabindex="-1">The cube layer <a class="header-anchor" href="#The-cube-layer" aria-label="Permalink to &quot;The cube layer {#The-cube-layer}&quot;">​</a></h2><p><code>AbstractCellLookup</code> makes a cell vector usable as a DimensionalData lookup, so a DGGS axis behaves like any other dimension. <code>Base.parent</code> returns the vector, and every cell verb a cube supports is defined once against that one method. Code meaning &quot;the cell dimension of this cube&quot; dispatches on this type and so accepts a cube from a store as readily as one built in memory.</p><p><code>CellLookup</code> is the lookup over a <code>CellVector</code>, <code>ChunkedCellLookup</code> the one over a stored axis. Slicing and selection work unmodified, and — the point of the exercise — a slice stays compressed whenever the result is still ascending. What a subset <em>is</em> stays each lookup&#39;s own: a computed window set stays a window set, and a stored axis stops being stored.</p><p><code>Cells</code> is the dimension. <code>Covering</code> is a selector that takes a geometry and returns the indices covering it.</p><h2 id="Store-IO" tabindex="-1">Store IO <a class="header-anchor" href="#Store-IO" aria-label="Permalink to &quot;Store IO {#Store-IO}&quot;">​</a></h2><p>Reading someone else&#39;s DGGS store is three stacked problems, and there is one abstraction for each.</p><p><code>CellEncoding</code> describes how cell identifiers are laid out on disk — densely, as ranges, or implicitly — together with the rank and select arithmetic needed to resolve a selector without scanning the identifier array.</p><p><code>DGGSConvention</code> describes how to recognise and interpret a particular community&#39;s metadata. Four ship. Detection runs every convention rather than stopping at the first match, sorts what they return so an explicit declaration beats a fingerprint, and merges the results field by field. Two conventions disagreeing about the same field is an error rather than a silent precedence choice.</p><p><code>StoreDescription</code> is the merged, format-neutral result. None of these three knows anything about Zarr; the Zarr dependency is weak and gates only the actual reading and writing.</p><p><code>ChunkedCellVector</code> is the axis those three produce, and it is an <code>AbstractCellVector</code>, so a cube from <code>dggread</code> is a region like any other. <code>region</code> converts it to the compressed form, and what that costs is the encoding&#39;s rather than the axis&#39;s length: a stored interval is a run of consecutive <em>ranks</em>, and a rank plus one is an index in the complete level grid, so the ranges and implicit encodings convert by arithmetic over what they already hold and read nothing. Only a dense axis has no such structure to borrow; its ids are read once, in the order that touches each chunk once, and the result is kept.</p><h2 id="Following-the-chunk-lines" tabindex="-1">Following the chunk lines <a class="header-anchor" href="#Following-the-chunk-lines" aria-label="Permalink to &quot;Following the chunk lines {#Following-the-chunk-lines}&quot;">​</a></h2><p>A cell-at-a-time pass over a lazy cube decodes one storage chunk per scalar read, so a ring that straddles a chunk boundary decodes its neighbour&#39;s chunk, drops it, and decodes it again for the next cell. The traversal that fixes this follows the chunk grid the store already has, and it is spelled as a plan and a runner rather than as one opaque call.</p><p><code>chunkplan</code> returns a <code>MapChunkPlan</code>: per chunk, the axis indices it owns and the axis indices outside it that its cells&#39; rings reach. Boundaries come from the data&#39;s own chunk grid, so an irregularly chunked store — one chunk per ancestor subtree, say — is planned on its real boundaries. Building a plan walks each chunk&#39;s boundary through <code>halo</code>, which is CPU and no IO.</p><p>The plan is a value because three decisions are made on it: the order to read in, how to cut it up — <code>split(plan, n)</code> is what parallelises a sweep, so there is no <code>threaded</code> keyword — and what it will cost before anything is read.</p><p><code>foreachchunk</code> runs it, handing the callback a <code>ChunkCube</code>: the chunk&#39;s cells <em>and its halo</em>, in memory, as an ordinary cube over a <code>CellLookup</code>. Every verb in the package then works on it unmodified. The owned cells are contiguous within it, because every halo cell is outside the chunk&#39;s own run, so <code>localindices</code> is a range and <code>globalindices</code> says where those results belong in the full axis.</p><p>Because the halo holds every axis neighbour of every owned cell, a stencil reaching no further than the plan&#39;s halo width computes on a chunk exactly what it computes on the whole axis. <code>mapneighbors!</code> is the streaming form built on that, and <code>mapneighbors</code> with <code>pass = Values()</code> takes the same route by itself whenever the data is chunked. <code>Neighbors()</code> deliberately does not: its callback closes over the original array and indexes it by the handles it is given, so a sweep over blocks would hand it block indices to read from the whole cube.</p><h2 id="Regridding" tabindex="-1">Regridding <a class="header-anchor" href="#Regridding" aria-label="Permalink to &quot;Regridding {#Regridding}&quot;">​</a></h2><p>Regridding lives in a separate package, <code>GlobalRegridding</code>, which owns the verbs and does not depend on this one. The main package implements its contract for DGGS grids by adding methods to imported bindings, so a session that loads both sees one function per name.</p><p>Its central abstraction is <code>RegridSpace</code>: anything with cells, chunks, and a manifold. A raster is one; a DGGS grid is another. Six methods are required of a space.</p><p>What a DGGS space contributes is the idea that a chunk is a non-empty ancestor subtree, chosen at whichever level lands closest to a target cell count. Chunk discovery is then a descent of the hierarchy the grid already has, rather than a second spatial index built alongside it.</p><p>Method, missing-data policy and weight storage are three orthogonal axes, each spelled as a singleton tag: <code>Conservative</code>, <code>NearestCell</code> or <code>BilinearPoint</code>; <code>Weighted</code> or <code>Extensive</code>; <code>PerChunk</code> or <code>Spilled</code>.</p><h2 id="Conformance-testing" tabindex="-1">Conformance testing <a class="header-anchor" href="#Conformance-testing" aria-label="Permalink to &quot;Conformance testing {#Conformance-testing}&quot;">​</a></h2><p>The abstractions above are prose. The executable form is <code>lib/DiscreteGlobalGridsConformanceTesting</code>, whose two entry points assert the laws a grid and a system must satisfy: index round-trips, boundary rings that are closed implicitly and wound counter-clockwise seen from outside, centroids strictly inside their own cells, neighbour symmetry and two-hop closure, ring and disc consistency, and the covering law — that a descendant&#39;s boundary lies inside every ancestor&#39;s node extent, at every depth.</p><p>A system opts out by capability rather than by name: the suite checks whether a method is actually specialised and skips the corresponding laws if it is not. A new grid or system is expected to pass both suites.</p>',68)])])}const g=t(i,[["render",n]]);export{u as __pageData,g as default};

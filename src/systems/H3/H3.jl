# ---------------------------------------------------------------------------
# T5 — H3. STUB.
#
# `H3Cell <: AbstractCellIndex` (UInt64, resolution in bits 52-55),
# `H3System <: AbstractHierarchicalGridSystem`. Port from `src/H3/` — the
# `H3Native` ccall layer copies over wholesale.
# ---------------------------------------------------------------------------

module H3

# TODO(T5): port the H3 system onto the new interface.

end # module H3

# ---------------------------------------------------------------------------
# T6 — HEALPix. STUB.
#
# Canonical id is `LevelIndex` over the nested pixel numbering; ring ordering
# is an alternate scheme via `reindex`. `HEALPixSystem <:
# AbstractHierarchicalGridSystem`, `has_sorted_subtrees = true`, and a
# `node_extent` OVERRIDE returning the exact subtree cap. Port from
# `src/HEALPix/`.
#
# Note the capitalisation: this submodule is `HEALPix`, distinct from the
# registered `Healpix.jl` package it wraps.
# ---------------------------------------------------------------------------

module HEALPix

# TODO(T6): port the HEALPix system onto the new interface.

end # module HEALPix

# ---------------------------------------------------------------------------
# T4 — IGeo7 (ISEA7H + Z7). STUB.
#
# `Z7Cell <: AbstractCellIndex` (UInt64, level in-band), `IGeo7System <:
# AbstractHierarchicalGridSystem`, the required interface plus the digit-
# arithmetic fast paths. Port from `src/IGeo7/` and `src/ISEA/` (which moves to
# `src/systems/ISEA/`, shared with the rest of the ISEA family).
# ---------------------------------------------------------------------------

module IGeo7

# TODO(T4): port the IGeo7 system onto the new interface.

end # module IGeo7

# The Python that test/io/xdggs_python.jl opens a `target = :xdggs` store with.
# xdggs is pinned so a red run means this package changed what it writes.
[deps]
xdggs = "0.6.*"
xarray = ""
zarr = ">=3"
